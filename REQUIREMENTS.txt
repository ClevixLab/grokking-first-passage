# Python dependencies — Grokking Paper Supplementary Material
# Tested with Python 3.10–3.12.
# Versions pinned for reproducibility; minor version updates should be safe.

# Deep learning framework
torch>=2.0,<3.0

# Numerical
numpy>=1.24,<3.0
scipy>=1.10,<2.0

# Plotting
matplotlib>=3.7,<4.0

# Optional: tqdm for progress bars
# tqdm>=4.65

# No other dependencies are needed.
# Recommended: install in a fresh venv via `bash setup_environment.sh`.
